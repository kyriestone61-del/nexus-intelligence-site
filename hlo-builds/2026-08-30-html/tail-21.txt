n('')}</ul></div></div>`;return}if(tab==='agents'){const registry=[['profile_interpreter','Profile Interpreter'],['opportunity_mapper','AI Opportunity Mapper'],['curriculum_architect','Curriculum Architect'],['mission_designer','Mission Designer'],['assessment_designer','Assessment Designer'],['resource_curator','Resource Curator'],['reality_qa','Reality & Safety QA']];host.innerHTML=`<div class="card"><div class="eyebrow">Curriculum agent team</div><h2>Seven specialized passes, one learner path</h2><p class="muted">When model access is configured, each role can run through the AI Gateway. Otherwise the same workflow uses a transparent structured fallback rather than pretending an AI call happened.</p><div class="agent-grid">${registry.map(([code,title])=>{const r=cloud.agentRuns.find(x=>x.curriculum_id===c.id&&x.agent_code===code);const st=r?.status||'not run';return `<div class="agent-card"><span class="agent-status">${esc(st)}</span><b>${esc(title)}</b><span class="note">${esc(r?.model||((st==='fallback')?'Structured fallback':'Awaiting run'))}</span></div>`}).join('')}</div></div>`;return}const p=cur.learner_profile||c.agent_summary?.profile_interpreter||{};const o=cur.opportunity_map||c.agent_summary?.opportunity_mapper||{};host.innerHTML=`<div class="grid2"><div class="card"><div class="eyebrow">Learner brief</div><h2>${esc(p.future_identity||c.title)}</h2><p><b>Primary goal:</b> ${esc(p.primary_goal||'')}</p><p><b>Building:</b> ${esc(p.what_they_are_building||'')}</p><p><b>AI level:</b> ${esc(p.current_ai_level||'')}</p><p><b>Weekly capacity:</b> ${esc(p.weekly_hours||'')} hours</p><p><b>Human strength:</b> ${esc(p.human_strength||'')}</p></div><div class="card"><div class="eyebrow">AI opportunity map</div><h2>Where leverage can compound</h2><ul>${(o.opportunities||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h3>Human moat</h3><div>${(o.human_moat||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div></div>`}
function renderPathResources(r){if(!r)return '';const items=r.resources||[];if(!items.length)return '';return `<div class="card" style="margin-top:12px"><div class="eyebrow">Recommended resources</div><h2>Go deeper without getting lost</h2>${items.map(x=>x.url?`<p><a href="${esc(x.url)}" target="_blank" rel="noopener"><b>${esc(x.title||x.type||'Resource')}</b></a></p>`:`<div class="path-module-block"><b>${esc(x.title||x.type||'Video topic')}</b><div class="note">Search: ${esc(x.query||'')}</div></div>`).join('')}</div>`}
function renderCustomModule(m,i){const p=customProgressFor(m.key)||{},status=p.status||'not_started',score=Math.round(Number(p.mastery_score)||0);return `<details class="path-module" ${i===0?'open':''}><summary><div><span class="badge">${esc(m.stage||'Personal')}</span><span class="badge">${esc(m.estimated_minutes||90)} min</span><h3 style="margin:8px 0 0">${i+1}. ${esc(m.title)}</h3></div><div><span class="pill">${esc(status.replaceAll('_',' '))}</span>${score?`<span class="pill">${score}%</span>`:''}</div></summary><div class="path-module-body"><p class="muted">${esc(m.why||'')}</p><div class="path-module-grid"><div class="path-module-block"><b>Outcomes</b><ul>${(m.outcomes||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="path-module-block"><b>AI applications</b><ul>${(m.ai_applications||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div>${(m.lessons||[]).map(l=>`<details class="academy-section"><summary>${esc(l.title||'Lesson')}</summary><div class="section-body"><p>${esc(l.teach||'')}</p><ol>${(l.steps||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol></div></details>`).join('')}<div class="callout"><b>Project:</b> ${esc(m.project||'Create a verified applied artifact.')}</div><div class="toolbar"><button class="secondary" onclick="window.HLOPersonal.startModule('${m.key}')">${status==='not_started'?'Start module':'Mark in progress'}</button><button class="secondary" onclick="window.HLOPersonal.submitEvidence('${m.key}')">Submit project evidence</button><button class="primary" onclick="window.HLOPersonal.quizModule('${m.key}')">Take module quiz</button></div><div id="customQuiz-${esc(m.key)}"></div><div class="note"><b>Mastery gate:</b> ${esc(m.mastery_gate||'Demonstrate understanding and evidence.')}</div></div></details>`}
async function setCustomProgress(key,status,score=null){if(!signedIn()||!cloud.activeCurriculum)return;const row={user_id:userId(),curriculum_id:cloud.activeCurriculum.id,module_key:key,status,updated_at:new Date().toISOString()};if(score!==null)row.mastery_score=score;if(['completed','mastered'].includes(status))row.completed_at=new Date().toISOString();const {data,error}=await sb.from('hlo_custom_module_progress').upsert(row,{onConflict:'user_id,curriculum_id,module_key'}).select().single();if(error){console.error(error);return toast('Progress save failed')}const idx=cloud.customProgress.findIndex(x=>x.curriculum_id===data.curriculum_id&&x.module_key===data.module_key);if(idx>=0)cloud.customProgress[idx]=data;else cloud.customProgress.push(data);renderPersonalPath();renderRecommendations();toast('Path progress saved')}
function startCustomModule(key){setCustomProgress(key,'in_progress')}
async function submitCustomEvidence(key){if(!signedIn()||!cloud.activeCurriculum)return;const note=prompt('Describe the project artifact you completed. Include a link, filename, result, or concise evidence note.');if(note===null||!note.trim())return;const cur=customProgressFor(key)||{};const evidence=[...(cur.evidence||[]),{note:note.trim(),recorded_at:new Date().toISOString()}];const score=Number(cur.mastery_score)||0;const row={user_id:userId(),curriculum_id:cloud.activeCurriculum.id,module_key:key,status:score>=80?'mastered':'completed',mastery_score:score,evidence,completed_at:new Date().toISOString(),updated_at:new Date().toISOString()};const {data,error}=await sb.from('hlo_custom_module_progress').upsert(row,{onConflict:'user_id,curriculum_id,module_key'}).select().single();if(error){console.error(error);return toast('Evidence save failed')}const i=cloud.customProgress.findIndex(x=>x.curriculum_id===data.curriculum_id&&x.module_key===data.module_key);if(i>=0)cloud.customProgress[i]=data;else cloud.customProgress.push(data);renderPersonalPath();renderRecommendations();toast(score>=80?'Module mastered':'Evidence saved — pass the quiz to master it')}
function quizCustomModule(key){const m=customModules().find(x=>x.key===key);if(!m)return;const qs=m.quiz||[];const host=$(`customQuiz-${key}`);if(!host||!qs.length)return toast('No quiz is attached to this module');let idx=0,score=0,ans=[];const draw=()=>{if(idx>=qs.length){const pct=Math.round(score/qs.length*100);host.innerHTML=`<div class="module-quiz"><div class="eyebrow">Result</div><h2>${pct}%</h2><p class="muted">${pct>=80?'Mastery threshold met. Finish the project artifact before treating the capability as proven.':'Review the misses and retry after applying the concept.'}</p></div>`;const ev=(customProgressFor(key)?.evidence||[]);setCustomProgress(key,(pct>=80&&ev.length)?'mastered':'in_progress',pct);if(signedIn())sb.from('hlo_quiz_attempts').insert({user_id:userId(),quiz_id:`custom:${cloud.activeCurriculum.id}:${key}`,lesson_id:key,difficulty:'adaptive',score:pct,answers:ans,weak_concepts:pct<80?[key]:[]});return}const q=qs[idx],choices=q.choices||[];host.innerHTML=`<div class="module-quiz"><div class="small muted">Question ${idx+1} of ${qs.length}</div><h3>${esc(q.question)}</h3>${choices.map((c,i)=>`<button class="module-quiz-choice" data-i="${i}">${esc(c)}</button>`).join('')}<div id="cqExplain"></div></div>`;host.querySelectorAll('.module-quiz-choice').forEach(b=>b.onclick=()=>{const pick=Number(b.dataset.i),ok=pick===Number(q.correct_index);if(ok)score++;ans.push({selected:pick,correct:ok});host.querySelectorAll('.module-quiz-choice').forEach((x,j)=>{x.disabled=true;if(j===Number(q.correct_index))x.classList.add('correct');else if(j===pick)x.classList.add('wrong')});const ex=host.querySelector('#cqExplain');if(ex)ex.innerHTML=`<div class="callout">${esc(q.explanation||'')}</div><button class="primary" id="cqNext-${key}">${idx===qs.length-1?'Finish':'Next'}</button>`;const nx=host.querySelector(`#cqNext-${CSS.escape(key)}`);if(nx)nx.onclick=()=>{idx++;draw()}})};draw()}
window.HLOPersonal={startModule:startCustomModule,quizModule:quizCustomModule,submitEvidence:submitCustomEvidence};

function renderProfile(){
  if(!cloud.profile)return;$('profileName').value=cloud.profile.display_name||'';$('profileHours').value=cloud.profile.weekly_learning_hours||5;$('profileCareer').value=cloud.profile.career_target||'';$('profileLevel').value=cloud.profile.experience_level||'beginner';$('profileGoal').value=cloud.profile.goal_statement||'';
}
async function saveProfile(){
  if(!signedIn())return;const row={user_id:userId(),display_name:$('profileName').value.trim(),weekly_learning_hours:Number($('profileHours').value)||5,career_target:$('profileCareer').value.trim()||null,experience_level:$('profileLevel').value,goal_statement:$('profileGoal').value.trim()||null,onboarding_complete:true,updated_at:new Date().toISOString()};
  const {data,error}=await sb.from('hlo_profiles').upsert(row,{onConflict:'user_id'}).select().single();if(error){console.error(error);$('profileMsg').textContent='Save failed.'}else{cloud.profile=data;$('profileMsg').textContent='Profile saved.';setAccount();renderRecommendations();toast()}
}

function renderAllCloud(){
  setAccount();applyAccessUI();renderPersonalPath();renderRecommendations();renderMissions();renderResearch();if(cloud.isOwner)renderTrades();renderTutorLessonOptions();renderProfile();evaluateCertificates();loadTutorHistory();if(signedIn())startAdaptiveQuiz();else if(window.restartQuiz)window.restartQuiz();
  if(window.HLOCore?.renderAcademy)window.HLOCore.renderAcademy();
}

async function signIn(){
  const email=$('authEmail').value.trim(),password=$('authPassword').value; $('authMsg').textContent='Signing in…';
  const {error}=await sb.a