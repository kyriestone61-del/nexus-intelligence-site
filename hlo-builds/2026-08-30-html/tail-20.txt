der','Leader / manager'],['creator','Creator'],['consultant','Consultant / independent professional'],['educator','Educator / coach'],['researcher','Researcher / analyst / investor'],['operator','Independent AI-leveraged operator']],
  loves:[['systems','Building systems'],['creating','Creating / designing'],['people','Working with people'],['research','Researching / analyzing'],['physical','Hands-on / physical work'],['leading','Selling / leading'],['teaching','Teaching / coaching'],['markets','Markets / finance']],
  build:[['business','A business'],['software','Software / app / automation'],['career','A career move or portfolio'],['media','An audience / media platform'],['service','A consulting or service offer'],['personal','A personal operating system'],['market','A market research / trading process'],['unknown','I am not sure yet']],
  domain:[['professional','Professional services'],['construction','Construction / trades / infrastructure'],['technology','Technology'],['creative','Creative / media'],['health','Healthcare / wellness'],['education','Education'],['finance','Finance / markets'],['manufacturing','Manufacturing / logistics'],['local','Local service business'],['general','Other / general']],
  ai_level:[['beginner','Brand new to AI'],['chat','Basic chat user'],['advanced','Daily advanced user'],['automation','Automation builder'],['agent','Agent / code builder']],
  learning_styles:[['steps','Step-by-step guides'],['projects','Project-based learning'],['videos','Short videos'],['cases','Examples / case studies'],['quizzes','Quizzes / challenges']],
  weekly_hours:[['1','About 1 hour'],['3','About 3 hours'],['5','About 5 hours'],['8','About 8 hours'],['10','10+ hours']],
  human_strength:[['judgment','Judgment / decision making'],['relationships','Relationships / empathy'],['creativity','Creativity / taste'],['leadership','Leadership / initiative'],['systems','Technical / systems thinking'],['physical','Hands-on adaptability'],['analytical','Analytical thinking']],
  risk_posture:[['conservative','Guardrails first'],['balanced','Balanced'],['experimental','Experimental / fast iteration']]
};
let builderStep=0,pathTab='curriculum';
function optionCards(key,host,multi=false){const h=$(host);if(!h)return;h.innerHTML=PATH_OPTIONS[key].map(([v,l])=>`<label class="choice-card"><input type="${multi?'checkbox':'radio'}" name="${key}" value="${v}"><span><b>${esc(l)}</b></span></label>`).join('')}
function initPathBuilder(){optionCards('goal','goalChoices');optionCards('future','futureChoices');optionCards('loves','loveChoices',true);optionCards('build','buildChoices');optionCards('domain','domainChoices');optionCards('ai_level','aiChoices');optionCards('learning_styles','learningChoices',true);optionCards('weekly_hours','hoursChoices');optionCards('human_strength','strengthChoices');optionCards('risk_posture','riskChoices');showBuilderStep(0)}
function showBuilderStep(n){builderStep=Math.max(0,Math.min(3,n));document.querySelectorAll('.builder-panel').forEach((p,i)=>p.classList.toggle('active',i===builderStep));document.querySelectorAll('.builder-step').forEach((x,i)=>{x.classList.toggle('active',i===builderStep);x.classList.toggle('done',i<builderStep)});if($('builderProgress'))$('builderProgress').style.width=`${(builderStep+1)*25}%`;if($('builderBack'))$('builderBack').style.visibility=builderStep?'visible':'hidden';if($('builderNext'))$('builderNext').textContent=builderStep===3?'Create My Curriculum':'Continue';if($('builderValidation'))$('builderValidation').textContent=''}
function selected(name,multi=false){const xs=[...document.querySelectorAll(`[name="${name}"]${multi?':checked':':checked'}`)].map(x=>x.value);return multi?xs:(xs[0]||'')}
function validateStep(){const req=builderStep===0?['goal','future']:builderStep===1?['loves','build','domain']:builderStep===2?['ai_level','learning_styles','weekly_hours']:['human_strength','risk_posture'];for(const k of req){const v=selected(k,['loves','learning_styles'].includes(k));if(Array.isArray(v)?!v.length:!v){$('builderValidation').textContent='Choose an answer for each question before continuing.';return false}}return true}
function intakeAnswers(){const goal=selected('goal'),future=selected('future'),build=selected('build'),domain=selected('domain'),ai=selected('ai_level'),hours=selected('weekly_hours');const label=(k,v)=>PATH_OPTIONS[k].find(x=>x[0]===v)?.[1]||v;return{goal:label('goal',goal),goal_code:goal,future:label('future',future),future_code:future,loves:selected('loves',true).map(v=>label('loves',v)),build:label('build',build),build_code:build,domain:label('domain',domain),domain_code:domain,ai_level:ai,ai_level_label:label('ai_level',ai),learning_styles:selected('learning_styles',true).map(v=>label('learning_styles',v)),weekly_hours:Number(hours)||5,human_strength:label('human_strength',selected('human_strength')),risk_posture:selected('risk_posture'),outcome:$('pathOutcome')?.value.trim()||''}}
async function buildPersonalCurriculum(){if(!signedIn())return toast('Sign in to create a persistent path');if(!validateStep())return;const btn=$('builderNext');btn.disabled=true;btn.textContent='Agent team is building…';$('builderValidation').textContent='Profile → opportunity map → curriculum → missions → assessments → resources → QA';try{const {data,error}=await sb.functions.invoke('hlo-curriculum-builder',{body:{answers:intakeAnswers()}});if(error)throw error;if(data?.error)throw new Error(data.error);await hydrate();C.go('personalize');toast(data.generation_mode==='ai_agents'?'AI agent curriculum created':'Personal curriculum created');}catch(e){console.error(e);$('builderValidation').textContent='Curriculum build failed: '+(e.message||String(e));}finally{btn.disabled=false;btn.textContent='Create My Curriculum'}}
function applyAccessUI(){document.querySelectorAll('.owner-only').forEach(el=>el.style.display=cloud.isOwner?'block':'none');const priv=['nexus','statecraft','career','trading','tradelog','plan'];if(!cloud.isOwner){priv.forEach(id=>document.getElementById(id)?.classList.add('private-hidden'));const active=document.querySelector('.view.active')?.id;if(priv.includes(active))C.go('dashboard')}else priv.forEach(id=>document.getElementById(id)?.classList.remove('private-hidden'));const f=$('missionTrackFilter');if(f){const desired=cloud.isOwner?['','Core Academy','Personal Path','Nexus Intelligence','Statecraft','Career','Trading']:['','Core Academy','Personal Path'];const cur=f.value;f.innerHTML=desired.map(x=>`<option value="${x}">${x||'All tracks'}</option>`).join('');if(desired.includes(cur))f.value=cur}if($('ownerDashboardCard'))$('ownerDashboardCard').style.display=cloud.isOwner?'block':'none';C.updateProgress()}
function customProgressFor(key){return cloud.customProgress.find(x=>x.curriculum_id===cloud.activeCurriculum?.id&&x.module_key===key)}
function pathCompletion(){const mods=customModules();if(!mods.length)return 0;return Math.round(mods.filter(m=>['completed','mastered'].includes(customProgressFor(m.key)?.status)).length/mods.length*100)}
function renderPersonalPath(){const c=cloud.activeCurriculum;if($('cmdPathStatus'))$('cmdPathStatus').textContent=c?`${pathCompletion()}%`:'Not built';if($('dashPathTitle'))$('dashPathTitle').textContent=c?c.title:'No personal curriculum yet';if($('dashPathSummary'))$('dashPathSummary').textContent=c?(c.summary||'Your personalized curriculum is active.'):'Answer a short set of questions about your future, interests, goals, preferred work and AI experience. The system will build a staged curriculum and practical projects around your answers.';if(!c){$('builderShell')?.style.setProperty('display','grid');if($('generatedPathHost'))$('generatedPathHost').style.display='none';if($('rebuildPathBtn'))$('rebuildPathBtn').style.display='none';if($('pathStatusScore'))$('pathStatusScore').textContent='Start';if($('pathStatusText'))$('pathStatusText').textContent='Answer the intake to create your first path.';return}if($('builderShell'))$('builderShell').style.display='none';if($('generatedPathHost'))$('generatedPathHost').style.display='block';if($('rebuildPathBtn'))$('rebuildPathBtn').style.display='inline-block';if($('pathStatusScore'))$('pathStatusScore').textContent=pathCompletion()+'%';if($('pathStatusText'))$('pathStatusText').textContent=`${c.title} · ${c.generation_mode.replace('_',' ')} · ${customModules().length} modules`;renderPathTab(pathTab)}
function renderPathTab(tab='curriculum'){pathTab=tab;document.querySelectorAll('#pathTabs button').forEach(b=>b.classList.toggle('active',b.dataset.pathTab===tab));const c=cloud.activeCurriculum,cur=c?.curriculum||{},host=$('pathTabContent');if(!c||!host)return;if(tab==='curriculum'){const mods=cur.modules||[];host.innerHTML=`<div class="card"><div class="eyebrow">Why this path</div><h2>${esc(c.title)}</h2><p class="muted">${esc(cur.path_rationale||c.summary||'')}</p><div>${(cur.priority_capabilities||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div>`+mods.map((m,i)=>renderCustomModule(m,i)).join('')+renderPathResources(cur.resources);return}if(tab==='plan'){host.innerHTML=`<div class="grid2"><div class="card"><div class="eyebrow">First 30 days</div><h2>Build momentum</h2>${(cur.thirty_day_plan||cur['30_day_plan']||[]).map(w=>`<div class="path-module-block"><b>Week ${esc(w.week||'')}</b><div>${esc(w.focus||'')}</div><div class="note">${esc(w.deliverable||'')}</div></div>`).join('')||'<p class="muted">Your modules are the plan: complete the first four in order.</p>'}</div><div class="card"><div class="eyebrow">90-day build</div><h2>${esc(cur.ninety_day_build?.outcome||cur['90_day_build']?.outcome||'Ship a real applied project')}</h2><p class="muted">${esc(cur.ninety_day_build?.proof||cur['90_day_build']?.proof||'Use a verified artifact as proof of capability.')}</p><h3>Practice loop</h3><ul>${(cur.practice_loops||['Learn → apply → verify → reflect','Quiz → review → retry','Build → test → revise']).map(x=>`<li>${esc(x)}</li>`).joi