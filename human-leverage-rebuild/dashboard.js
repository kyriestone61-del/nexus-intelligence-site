import{S,$,pct,bestQuiz,completedLessons,nextRecommendation,go,esc}from'https://dmdgkjksouhhsuojthav.supabase.co/functions/v1/hlo-app?asset=core.js';
export function renderDashboard(){const done=completedLessons(),r=nextRecommendation(),corePct=pct(done.size,S.modules.length);$('#dashboard').innerHTML=`<div class="hero"><div class="card"><div class="eyebrow">Human Leverage OS</div><h2>Build an <span class="gold">AI-leveraged future</span> specific to you.</h2><p class="muted">Learn the universal foundations of AI, automation, agents and robotics; strengthen the human capabilities that become more valuable as machine intelligence gets cheaper; then build a personal path around what you want to create and the person you want to become.</p><div class="toolbar"><button class="btn primary" id="dashPath">Build My Path</button><button class="btn" id="dashAcademy">Open Core Academy</button></div></div><div class="card"><div class="eyebrow">Your progress</div><div class="score">${corePct}%</div><div class="progress"><div style="width:${corePct}%"></div></div><div style="margin-top:10px"><div class="kpi"><span>Core modules</span><b>${done.size}/${S.modules.length}</b></div><div class="kpi"><span>Personal path</span><b>${S.curriculum?'Built':'Not built'}</b></div><div class="kpi"><span>Quiz best</span><b>${bestQuiz()||'—'}%</b></div><div class="kpi"><span>Active missions</span><b>${S.missions.filter(x=>['active','in_progress'].includes(x.status)).length}</b></div></div></div></div><div class="grid two" style="margin-top:16px"><div class="card reco"><div class="eyebrow">Next best action · ${r.type}</div><h2>${esc(r.title)}</h2><p class="muted">${esc(r.reason)}</p><button class="btn primary" id="doNext">Go</button></div><div class="card"><div class="eyebrow">The operating loop</div><h3>Intent → Machine leverage → Human verification → Evidence → Adaptation</h3><p class="muted">The system does not reward consumption alone. Read, watch, apply, test, produce evidence, and use the result to decide what to learn or build next.</p></div></div><div class="grid three" style="margin-top:16px"><div class="card"><div class="eyebrow">Human moat</div><h3>Judgment + trust</h3><p class="muted">Practice ambiguity management, verification, relationships, taste, accountability and cross-domain synthesis.</p></div><div class="card"><div class="eyebrow">Machine leverage</div><h3>Orchestrate systems</h3><p class="muted">Move from prompting to workflows, agents, APIs, retrieval, evaluation and bounded automation.</p></div><div class="card"><div class="eyebrow">Economic objective</div><h3>Own the upside</h3><p class="muted">Turn productivity into systems, IP, distribution, products, equity or other scalable assets rather than only more output.</p></div></div>`;$('#dashPath').onclick=()=>go('personalize');$('#dashAcademy').onclick=()=>go('academy');$('#doNext').onclick=()=>go(r.view)}
