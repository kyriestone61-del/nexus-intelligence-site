import{S,$,sb,esc,toast,go}from'https://dmdgkjksouhhsuojthav.supabase.co/functions/v1/hlo-app?asset=core.js';

const DIMS=[
 {key:'domain_expertise',id:'levDomain',label:'Domain expertise',help:'Can you solve valuable problems in a domain with depth, context and judgment?'},
 {key:'judgment_verification',id:'levJudgment',label:'Judgment & verification',help:'Can you tell when AI output is trustworthy, incomplete, risky or wrong?'},
 {key:'systems_automation',id:'levSystems',label:'Systems & automation',help:'How much of your useful work is encoded into repeatable workflows instead of raw effort?'},
 {key:'relationships_distribution',id:'levDistribution',label:'Relationships & distribution',help:'Do you have reliable ways to reach people who value what you know or build?'},
 {key:'owned_ip_data_assets',id:'levAssets',label:'Owned IP / data / assets',help:'How much value keeps working after the original task is finished?'},
 {key:'capital_allocation',id:'levCapital',label:'Capital allocation',help:'Do you deliberately redirect time, money and attention toward the highest-return assets?'}
];
let diagnostic=null,snapshots=[];

function styles(){if(document.getElementById('leverageV2Styles'))return;const s=document.createElement('style');s.id='leverageV2Styles';s.textContent=`
.leverage-shell{max-width:1050px;margin:0 auto}.lev-hero{padding:28px;border:1px solid rgba(216,180,90,.28);background:linear-gradient(145deg,rgba(216,180,90,.10),var(--surface) 52%);border-radius:18px}.lev-hero h2{font-size:34px;line-height:40px;letter-spacing:-.035em;margin:5px 0 10px}.lev-hero p{max-width:820px;margin:0}.lev-metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;background:var(--line);border:1px solid var(--line);border-radius:14px;overflow:hidden;margin:18px 0 0}.lev-metric{background:var(--surface);padding:18px;min-height:112px;display:grid;align-content:start;gap:3px}.lev-metric span{font-size:11px;color:var(--tertiary);font-weight:750}.lev-metric strong{font-size:27px;line-height:32px;letter-spacing:-.03em}.lev-metric small{font-size:12px;color:var(--muted)}.lev-focus{margin-top:18px;padding:22px;border-left:3px solid var(--gold);background:var(--gold-soft);border-radius:10px}.lev-focus h3{font-size:23px;margin:3px 0 8px}.lev-actions{display:grid;gap:10px;margin-top:14px}.lev-action{border:1px solid var(--line);border-radius:12px;padding:16px;background:var(--surface)}.lev-action:first-child{border-color:rgba(216,180,90,.34)}.lev-action-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start}.lev-action h3{font-size:17px;margin:2px 0 6px}.lev-action p{font-size:13px;margin:0;color:var(--muted);line-height:1.55}.lev-action .toolbar{margin-top:12px}.lev-dims{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.lev-dim{border:1px solid var(--line);border-radius:12px;padding:16px;background:var(--surface)}.lev-dim-head{display:flex;justify-content:space-between;gap:12px}.lev-dim h4{margin:0;font-size:15px}.lev-score{font-size:22px;font-weight:850}.lev-bar{height:7px;background:#20262e;border-radius:99px;overflow:hidden;margin:10px 0 7px}.lev-bar>span{display:block;height:100%;background:var(--gold)}.lev-confidence{font-size:11px;color:var(--tertiary)}.lev-evidence{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.lev-evidence>div{padding:12px;border:1px solid var(--line);border-radius:10px}.lev-evidence b{font-size:20px;display:block}.lev-evidence span{font-size:11px;color:var(--tertiary)}.lev-snapshot{border:1px solid var(--line);border-radius:14px;background:var(--surface);margin-top:20px}.lev-snapshot summary{cursor:pointer;padding:18px 20px;font-weight:800}.lev-snapshot-body{padding:0 20px 20px}.lev-slider{border-top:1px solid var(--line);padding:15px 0}.lev-slider:first-child{border-top:0}.lev-slider-head{display:flex;justify-content:space-between;gap:12px}.lev-slider-head label{font-weight:750}.lev-slider-head output{font-weight:850;color:var(--gold-hover)}.lev-slider p{font-size:12px;color:var(--muted);margin:4px 0 8px}.lev-slider input[type=range]{width:100%;accent-color:var(--gold)}.lev-anchors{display:flex;justify-content:space-between;font-size:10px;color:var(--tertiary);gap:5px}.lev-history{display:grid;gap:8px}.lev-history-row{display:grid;grid-template-columns:110px 90px 1fr;gap:14px;align-items:center;padding:10px 0;border-top:1px solid var(--line)}.lev-history-row:first-child{border-top:0}.lev-spark{width:100%;height:90px;display:block}.lev-stage{color:var(--gold-hover)}@media(max-width:800px){.lev-metrics{grid-template-columns:repeat(2,1fr)}.lev-dims{grid-template-columns:1fr}.lev-evidence{grid-template-columns:repeat(2,1fr)}.lev-action-head{display:block}.lev-history-row{grid-template-columns:90px 70px 1fr}}`;
document.head.appendChild(s)}

function signed(){return!!S.user}
function fmtMomentum(v){if(v===null||v===undefined)return'Baseline';const n=Number(v)||0;return`${n>0?'+':''}${n}`}
function stageCopy(){return diagnostic?.stage?.label||'Not diagnosed'}
function sparkline(trend=[]){if(trend.length<2)return'';const w=600,h=80,p=6,scores=trend.map(x=>Number(x.score)||0),min=Math.min(...scores),max=Math.max(...scores),range=Math.max(1,max-min);const pts=scores.map((v,i)=>`${p+(i/(scores.length-1))*(w-p*2)},${h-p-((v-min)/range)*(h-p*2)}`).join(' ');return`<svg class="lev-spark" viewBox="0 0 ${w} ${h}" role="img" aria-label="Leverage score trend from ${scores[0]} to ${scores[scores.length-1]}"><polyline points="${pts}" fill="none" stroke="currentColor" stroke-width="3" vector-effect="non-scaling-stroke" style="color:var(--gold)"/></svg>`}
function dimCards(){const ds=diagnostic?.dimensions||{};return DIMS.map(d=>{const x=ds[d.key]||{},score=Number(x.score)||0,conf=Number(x.confidence)||0;return`<div class="lev-dim"><div class="lev-dim-head"><div><h4>${esc(d.label)}</h4><div class="small muted">${esc(d.help)}</div></div><div class="lev-score">${score}</div></div><div class="lev-bar"><span style="width:${score}%"></span></div><div class="lev-confidence">Evidence confidence ${conf}%</div></div>`}).join('')}
function observedEvidence(){const e=diagnostic?.observed_evidence||{};const rows=[['Capability',e.capability_score],['Mastered modules',e.mastered_modules],['Completed labs',e.unique_labs],['Completed missions',e.completed_missions],['Mature opportunities',e.mature_opportunities],['Allocation decisions',e.allocation_decisions],['Recent quiz avg',`${e.recent_quiz_average||0}%`],['Active missions',e.active_missions]];return rows.map(([l,v])=>`<div><b>${esc(v??0)}</b><span>${esc(l)}</span></div>`).join('')}
function actionCards(){const actions=diagnostic?.actions||[];return actions.map((a,i)=>`<div class="lev-action"><div class="lev-action-head"><div><div class="eyebrow">${i===0?'HIGHEST-RETURN MOVE':'NEXT OPTION'}</div><h3>${esc(a.title)}</h3><p>${esc(a.reason||'')}</p></div>${a.dimension?`<span class="pill">${esc(String(a.dimension).replaceAll('_',' '))}</span>`:''}</div><div class="callout" style="margin:10px 0 0"><b>Do this:</b> ${esc(a.action||'')}</div>${a.success_metric?`<div class="small muted" style="margin-top:8px"><b>Done when:</b> ${esc(a.success_metric)}</div>`:''}<div class="toolbar"><button class="btn primary make-lev-mission" data-action="${i}">Make this a mission</button>${a.view&&a.view!=='leverage'?`<button class="btn ghost open-lev-action" data-view="${esc(a.view)}">Open ${esc(a.view==='academy'?'Academy':a.view==='labs'?'Labs':a.view==='opportunities'?'Opportunity Engine':'Practice')}</button>`:''}</div></div>`).join('')}
function historyRows(){const trend=diagnostic?.trend||[];if(!trend.length)return'';return trend.slice().reverse().slice(0,6).map((x,i,arr)=>{const next=arr[i+1],delta=next?Number(x.score)-Number(next.score):null;return`<div class="lev-history-row"><div class="small muted">${new Date(x.created_at).toLocaleDateString()}</div><b>${x.score}</b><div class="small ${delta===null?'muted':delta>0?'':'muted'}">${delta===null?'Baseline':`${delta>0?'+':''}${delta} from prior`}</div></div>`}).join('')}
function snapshotForm(latest={}){return`<details class="lev-snapshot" ${diagnostic?.has_snapshot?'':'open'}><summary>${diagnostic?.has_snapshot?'Update my leverage snapshot':'Create my baseline'}</summary><div class="lev-snapshot-body"><p class="muted small">Use the anchors instead of chasing precision: <b>0 = none · 25 = emerging · 50 = repeatable · 75 = scalable · 100 = compounding.</b> The diagnostic separately measures how much observable evidence supports your ratings.</p>${DIMS.map(d=>{const v=Number(latest[d.key])||0;return`<div class="lev-slider"><div class="lev-slider-head"><label for="${d.id}">${esc(d.label)}</label><output id="${d.id}Out">${v}</output></div><p>${esc(d.help)}</p><input id="${d.id}" type="range" min="0" max="100" step="5" value="${v}" aria-describedby="${d.id}Help"><div id="${d.id}Help" class="lev-anchors"><span>None</span><span>Emerging</span><span>Repeatable</span><span>Scalable</span><span>Compounding</span></div></div>`}).join('')}<div class="field"><label for="levEvidence">${diagnostic?.has_snapshot?'What changed, and what evidence supports the change?':'What evidence supports this baseline?'}</label><textarea id="levEvidence" placeholder="Name artifacts, results, responsibilities, systems, audience/customer signals, owned assets, or allocation decisions."></textarea></div><div class="toolbar" style="margin-top:12px"><button id="saveLeverageV2" class="btn primary">${diagnostic?.has_snapshot?'Update diagnosis':'Create baseline & diagnose'}</button></div></div></details>`}

export async function renderLeverage(){styles();const host=$('#leverage');if(!host)return;if(!signed()){host.innerHTML='<div class="card"><div class="eyebrow">Leverage Diagnostic</div><h2>Sign in to diagnose your leverage.</h2></div>';return}host.innerHTML='<div class="card muted">Analyzing your leverage, evidence and current constraints…</div>';const[{data:d,error:de},{data:ss,error:se}]=await Promise.all([sb.rpc('hlo_leverage_diagnostic'),sb.from('hlo_leverage_snapshots').select('*').eq('user_id',S.user.id).order('created_at',{ascending:false}).limit(12)]);if(de||se){host.innerHTML=`<div class="card"><h2>Leverage Diagnostic unavailable</h2><p class="muted">${esc((de||se)?.message||'Could not load the diagnostic.')}</p></div>`;return}diagnostic=d||{has_snapshot:false};S.leverageDiagnostic=diagnostic;snapshots=ss||[];const latest=diagnostic.latest_snapshot||snapshots[0]||{};
 if(!diagnostic.has_snapshot){host.innerHTML=`<div class="leverage-shell"><div class="lev-hero"><div class="eyebrow">Leverage Diagnostic</div><h2>Find the constraint that is keeping your capability from compounding.</h2><p class="muted">The point is not to collect a score. Your baseline lets Human OS identify your weakest leverage dimension, then future activity and snapshots show whether knowledge is becoming systems, distribution, owned assets and better allocation decisions.</p><div class="callout"><b>After the baseline:</b> you get a bottleneck, a stage, an evidence-confidence score, a knowledge→leverage conversion score, and concrete next actions you can turn into missions.</div></div>${snapshotForm(latest)}</div>`;wire();return}
 const b=diagnostic.bottleneck||{},trend=diagnostic.trend||[];host.innerHTML=`<div class="leverage-shell"><div class="lev-hero"><div class="eyebrow">Leverage Diagnostic</div><h2>Your constraint is <span class="lev-stage">${esc(b.label||'not yet clear')}</span>.</h2><p class="muted">This page combines your self-audit with observable activity across Human OS. It is designed to tell you what is preventing your knowledge from turning into durable leverage—and what to do about it next.</p><div class="lev-metrics"><div class="lev-metric"><span>LEVERAGE SCORE</span><strong>${diagnostic.score}</strong><small>Bottleneck-weighted, not a simple average</small></div><div class="lev-metric"><span>CURRENT STAGE</span><strong>${esc(stageCopy())}</strong><small>${esc(diagnostic.stage?.explanation||'')}</small></div><div class="lev-metric"><span>MOMENTUM</span><strong>${esc(fmtMomentum(diagnostic.momentum))}</strong><small>${diagnostic.momentum===null?'First measurement':'vs. previous snapshot'}</small></div><div class="lev-metric"><span>KNOWLEDGE → LEVERAGE</span><strong>${diagnostic.conversion_score}%</strong><small>How much expertise is becoming systems, reach and assets</small></div></div><div class="lev-focus"><div class="eyebrow">CURRENT CONSTRAINT</div><h3>${esc(b.label||'—')} · ${Number(b.score)||0}/100</h3><p class="muted">This is ${Number(b.gap_to_average)||0} points below your current average. Strengthening the constraint usually creates more value than adding another strength on top of an already strong dimension.</p><div class="small" style="margin-top:8px"><b>Evidence confidence:</b> ${diagnostic.evidence_confidence}% — this measures how much of the diagnosis is supported by activity/artifacts versus self-rating.</div></div></div><div class="surface-section" style="margin-top:28px"><div class="section-head"><div><div class="eyebrow">WHAT TO DO NEXT</div><h3>Turn the diagnosis into action</h3></div><button id="askTutorLeverage" class="btn ghost">Ask Tutor about this</button></div><div class="lev-actions">${actionCards()}</div></div><div class="surface-section" style="margin-top:30px"><div class="eyebrow">SIX LEVERAGE DIMENSIONS</div><h3>Where capability is strong—and where it is fragile</h3><div class="lev-dims">${dimCards()}</div></div><div class="surface-section" style="margin-top:30px"><div class="eyebrow">OBSERVED EVIDENCE</div><h3>What Human OS can actually see</h3><p class="muted small">Self-ratings are never treated as objective truth. These signals increase confidence in the diagnosis.</p><div class="lev-evidence">${observedEvidence()}</div></div>${trend.length>1?`<div class="surface-section" style="margin-top:30px"><div class="eyebrow">MOMENTUM</div><h3>Are you actually compounding?</h3>${sparkline(trend)}<div class="lev-history">${historyRows()}</div></div>`:''}${snapshotForm(latest)}</div>`;wire()}

function wire(){DIMS.forEach(d=>{const input=$('#'+d.id),out=$('#'+d.id+'Out');if(input&&out)input.oninput=()=>out.textContent=input.value});if($('#saveLeverageV2'))$('#saveLeverageV2').onclick=saveSnapshot;document.querySelectorAll('.make-lev-mission').forEach(b=>b.onclick=()=>makeMission(Number(b.dataset.action)));document.querySelectorAll('.open-lev-action').forEach(b=>b.onclick=()=>go(b.dataset.view));if($('#askTutorLeverage'))$('#askTutorLeverage').onclick=askTutor}
async function saveSnapshot(){const n=id=>Math.max(0,Math.min(100,Number($('#'+id)?.value)||0)),note=$('#levEvidence')?.value.trim()||'';if(note.length<20)return toast('Add a short evidence note so the diagnosis is grounded in something observable.');const row={user_id:S.user.id,domain_expertise:n('levDomain'),judgment_verification:n('levJudgment'),systems_automation:n('levSystems'),relationships_distribution:n('levDistribution'),owned_ip_data_assets:n('levAssets'),capital_allocation:n('levCapital'),evidence_note:note,created_at:new Date().toISOString()};const{error}=await sb.from('hlo_leverage_snapshots').insert(row);if(error)return toast(error.message);toast('Snapshot saved · diagnosis updated');renderLeverage()}
async function makeMission(i){const a=(diagnostic?.actions||[])[i];if(!a)return;const row={user_id:S.user.id,track:'Personal Path',title:a.title,description:a.action||'',mission_type:'leverage',difficulty:'operator',status:'active',priority:1,acceptance_criteria:a.success_metric?[a.success_metric]:[],evidence:{source:'leverage_balance_sheet',dimension:a.dimension||null,diagnostic_score:diagnostic.score},created_at:new Date().toISOString(),updated_at:new Date().toISOString()};const{error}=await sb.from('hlo_missions').insert(row);if(error)return toast(error.message);toast('Added to Missions')}
function askTutor(){const b=diagnostic?.bottleneck||{},prompt=`My Leverage Diagnostic says my current stage is ${stageCopy()}, my leverage score is ${diagnostic.score}, my knowledge-to-leverage conversion is ${diagnostic.conversion_score}%, and my main constraint is ${b.label} at ${b.score}/100. Explain why this constraint matters, challenge the diagnosis if the evidence is weak, and guide me through the highest-return next action one step at a time.`;go('tutor');setTimeout(()=>{const input=$('#tutorInput');if(input){input.value=prompt;input.focus()}},80)}
